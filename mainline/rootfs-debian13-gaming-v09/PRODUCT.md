# R46H gaming product p2 v0.9

This image keeps the p2 v0.8 ES-DE, Ozone, Neo-Geo fallback, screenshots and
remote input unchanged. It adds one pinned full FBNeo core and exposes the
read-only original-card `arcade` directory in ES-DE. ROMs remain on p3 and no
operator key is embedded.

# R46H gaming product p2 v0.17

This image keeps exact p2 v0.16 and adds root-level ES-DE `<loadExclusive/>` so
only the reviewed 13-system custom list is loaded. Dreamcast stays hidden while
its diagnostic core and content remain installed.

--- a/src/core/timebars.h
+++ b/src/core/timebars.h
@@ -3 +3,15 @@
-#ifdef TIMEBARS
+#if defined(TIMEBARS) || defined(MASTER)
+enum eR46HPreRenderProfile
+{
+	R46H_PRERENDER_BUILDING,
+	R46H_PRERENDER_VEHICLE,
+	R46H_PRERENDER_PED,
+	R46H_PRERENDER_OBJECT,
+	R46H_PRERENDER_OTHER,
+	R46H_PRERENDER_INVISIBLE,
+	R46H_PRERENDER_ALPHA,
+	R46H_PRERENDER_HELI,
+	R46H_PRERENDER_SHADOWS,
+	R46H_PRERENDER_COUNT,
+};
+
@@ -7,0 +22,2 @@
+uint64 tbProfileNowNs();
+void tbAddPreRenderTime(int32, uint64);
--- a/src/core/timebars.cpp
+++ b/src/core/timebars.cpp
@@ -1,0 +2 @@
+#include "timebars.h"
@@ -2 +2,121 @@
-#ifndef MASTER
+#ifdef MASTER
+#include <stdio.h>
+#include <string.h>
+#include <time.h>
+
+struct R46HProfileTimer
+{
+	const char *name;
+	const char *key;
+	uint64 startNs;
+	uint64 totalNs;
+};
+
+static R46HProfileTimer r46hTimers[] = {
+	{ "CGame::Process", "process" },
+	{ "DMAudio.Service", "audio" },
+	{ "CnstrRenderList", "construct" },
+	{ "PreRender", "prerender" },
+	{ "BeginFrame", "begin" },
+	{ "RenderScene", "scene" },
+	{ "Effects", "effects" },
+	{ "RenderMotionBlur", "blur" },
+	{ "Render2dStuff", "hud" },
+	{ "RenderMenus", "menus" },
+	{ "DoFade", "fade" },
+	{ "Render2dStuff-Fade", "afterfade" },
+	{ "Swap", "swap" },
+};
+static const char *r46hPreRenderKeys[R46H_PRERENDER_COUNT] = {
+	"pre_building", "pre_vehicle", "pre_ped", "pre_object", "pre_other",
+	"pre_invisible", "pre_alpha", "pre_heli", "pre_shadows",
+};
+static uint64 r46hPreRenderNs[R46H_PRERENDER_COUNT];
+static uint32 r46hPreRenderCalls[R46H_PRERENDER_COUNT];
+static uint64 r46hFrameStartNs, r46hWindowStartNs, r46hFrameTotalNs;
+static uint32 r46hFrameCount;
+
+static uint64
+r46hNowNs(void)
+{
+	struct timespec ts;
+	clock_gettime(CLOCK_MONOTONIC, &ts);
+	return (uint64)ts.tv_sec*1000000000ULL + ts.tv_nsec;
+}
+
+uint64
+tbProfileNowNs(void)
+{
+	return r46hNowNs();
+}
+
+void
+tbAddPreRenderTime(int32 category, uint64 startNs)
+{
+	if(category < 0 || category >= R46H_PRERENDER_COUNT)
+		return;
+	r46hPreRenderNs[category] += r46hNowNs() - startNs;
+	r46hPreRenderCalls[category]++;
+}
+
+void
+tbInit(void)
+{
+	r46hFrameStartNs = r46hNowNs();
+	if(r46hWindowStartNs == 0)
+		r46hWindowStartNs = r46hFrameStartNs;
+	for(uint32 i = 0; i < ARRAY_SIZE(r46hTimers); i++)
+		r46hTimers[i].startNs = 0;
+}
+
+void
+tbStartTimer(int32, Const char *name)
+{
+	for(uint32 i = 0; i < ARRAY_SIZE(r46hTimers); i++)
+		if(strcmp(name, r46hTimers[i].name) == 0){
+			r46hTimers[i].startNs = r46hNowNs();
+			return;
+		}
+}
+
+void
+tbEndTimer(Const char *name)
+{
+	uint64 now = r46hNowNs();
+	for(uint32 i = 0; i < ARRAY_SIZE(r46hTimers); i++)
+		if(strcmp(name, r46hTimers[i].name) == 0 && r46hTimers[i].startNs != 0){
+			r46hTimers[i].totalNs += now - r46hTimers[i].startNs;
+			r46hTimers[i].startNs = 0;
+			return;
+		}
+}
+
+void
+tbDisplay(void)
+{
+	uint64 now = r46hNowNs();
+	r46hFrameTotalNs += now - r46hFrameStartNs;
+	r46hFrameCount++;
+	if(now - r46hWindowStartNs < 1000000000ULL)
+		return;
+	fprintf(stderr, "R46H_FRAME_PROFILE frames=%u frame_us=%llu", r46hFrameCount,
+		(unsigned long long)(r46hFrameTotalNs/r46hFrameCount/1000));
+	for(uint32 i = 0; i < ARRAY_SIZE(r46hTimers); i++){
+		fprintf(stderr, " %s_us=%llu", r46hTimers[i].key,
+			(unsigned long long)(r46hTimers[i].totalNs/r46hFrameCount/1000));
+		r46hTimers[i].totalNs = 0;
+	}
+	for(uint32 i = 0; i < R46H_PRERENDER_COUNT; i++){
+		fprintf(stderr, " %s_us=%llu %s_calls=%u", r46hPreRenderKeys[i],
+			(unsigned long long)(r46hPreRenderNs[i]/r46hFrameCount/1000),
+			r46hPreRenderKeys[i], r46hPreRenderCalls[i]/r46hFrameCount);
+		r46hPreRenderNs[i] = 0;
+		r46hPreRenderCalls[i] = 0;
+	}
+	fputc('\n', stderr);
+	fflush(stderr);
+	r46hFrameTotalNs = 0;
+	r46hFrameCount = 0;
+	r46hWindowStartNs = now;
+}
+#else
@@ -121 +241 @@
-#endif // !MASTER
\ No newline at end of file
+#endif // MASTER
--- a/src/core/main.cpp
+++ b/src/core/main.cpp
@@ -1599,0 +1600 @@
+		tbStartTimer(0, "BeginFrame");
@@ -1608,0 +1610 @@
+		tbEndTimer("BeginFrame");
@@ -1620 +1622 @@
-
+		tbStartTimer(0, "Effects");
@@ -1626,0 +1629 @@
+		tbEndTimer("Effects");
@@ -1679,3 +1682 @@
-	if (gbShowTimebars)
-		tbDisplay();
-
+	tbStartTimer(0, "Swap");
@@ -1682,0 +1684,2 @@
+	tbEndTimer("Swap");
+	tbDisplay();
--- a/src/renderer/Renderer.cpp
+++ b/src/renderer/Renderer.cpp
@@ -27,0 +28 @@
+#include "timebars.h"
@@ -88,0 +90,20 @@
+static int32
+r46hPreRenderCategory(CEntity *entity)
+{
+	switch(entity->GetType()){
+	case ENTITY_TYPE_BUILDING: return R46H_PRERENDER_BUILDING;
+	case ENTITY_TYPE_VEHICLE: return R46H_PRERENDER_VEHICLE;
+	case ENTITY_TYPE_PED: return R46H_PRERENDER_PED;
+	case ENTITY_TYPE_OBJECT: return R46H_PRERENDER_OBJECT;
+	default: return R46H_PRERENDER_OTHER;
+	}
+}
+
+static void
+r46hPreRenderEntity(CEntity *entity, int32 category)
+{
+	uint64 startNs = tbProfileNowNs();
+	entity->PreRender();
+	tbAddPreRenderTime(category, startNs);
+}
+
@@ -96 +117 @@
-		ms_aVisibleEntityPtrs[i]->PreRender();
+		r46hPreRenderEntity(ms_aVisibleEntityPtrs[i], r46hPreRenderCategory(ms_aVisibleEntityPtrs[i]));
@@ -101 +122 @@
-			ms_aVisibleVehiclePtrs[i]->PreRender();
+			r46hPreRenderEntity(ms_aVisibleVehiclePtrs[i], R46H_PRERENDER_VEHICLE);
@@ -104 +125 @@
-			ms_aVisibleBuildingPtrs[i]->PreRender();
+			r46hPreRenderEntity(ms_aVisibleBuildingPtrs[i], R46H_PRERENDER_BUILDING);
@@ -108 +129 @@
-			((CEntity*)node->item.entity)->PreRender();
+			r46hPreRenderEntity((CEntity*)node->item.entity, R46H_PRERENDER_BUILDING);
@@ -116 +137 @@
-		ms_aInVisibleEntityPtrs[i]->PreRender();
+		r46hPreRenderEntity(ms_aInVisibleEntityPtrs[i], R46H_PRERENDER_INVISIBLE);
@@ -122 +143 @@
-		((CEntity*)node->item.entity)->PreRender();
+		r46hPreRenderEntity((CEntity*)node->item.entity, R46H_PRERENDER_ALPHA);
@@ -123,0 +145 @@
+	uint64 startNs = tbProfileNowNs();
@@ -124,0 +147,2 @@
+	tbAddPreRenderTime(R46H_PRERENDER_HELI, startNs);
+	startNs = tbProfileNowNs();
@@ -125,0 +150 @@
+	tbAddPreRenderTime(R46H_PRERENDER_SHADOWS, startNs);
